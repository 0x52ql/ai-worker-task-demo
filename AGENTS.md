# 数字员工工作指南

本文件位于仓库根目录，定义“内部任务管理 Demo”的工作规则，适用于整个仓库。处理子目录文件时，还应读取路径上更具体的 AGENTS.md；更具体的规则在对应范围内优先适用。

## 开始任务前

1. 阅读 README.md，了解项目状态和运行方式。
2. 阅读 [docs/specs.md](./docs/specs.md) 和 [docs/plan.md](./docs/plan.md)，确认功能范围、验收标准和执行顺序。
3. 检查当前分支、工作区改动和已有实现，保留他人未提交的工作。
4. 基于指定基础分支开展工作；本示例建议使用 `feat/task-management-demo`，已有任务分支时继续使用。

## 文档职责

- docs/specs.md 定义功能、数据规则和验收标准。
- docs/plan.md 定义实施步骤、依赖和完成状态。
- AGENTS.md 定义仓库内的工作规则。
- README.md 说明项目用途、当前状态、运行方式和已知限制。

如果需求文件之间存在影响实现的矛盾，明确指出冲突并取得需求结论，随后同步文档。不得通过删改验收标准来让未完成的实现看似通过。

## 实现约定

- 本 Demo 使用 React、TypeScript、Vite 和 npm，提交 package-lock.json。
- 优先沿用现有代码组织和样式，不为简单功能增加额外框架。
- 保持任务数据、存储逻辑和页面交互职责清晰。
- 用户输入按纯文本显示，遵循 Spec 中的标题校验和异常处理要求。
- 不提交密钥、访问令牌、真实业务数据、node_modules 或本地临时文件。
- 仅修改与当前任务相关的内容，不覆盖或撤销他人的改动。

## 执行与沟通

- 按 Plan 推进，完成并验证后再勾选对应条目。
- 常规实现细节自行决定；涉及功能范围、数据规则或验收标准的调整，先明确需求并更新文档。
- 遇到权限、依赖或信息缺失时，记录具体阻塞，并继续完成不受影响的工作。
- 除非任务已明确授权，不合并主分支、不部署生产环境、不引入付费服务。

## 验证要求

应用初始化后，交付前运行：

```bash
npm ci
npm run typecheck
npm test -- --run
npm run build
```

- 测试重点覆盖标题边界、状态筛选、刷新恢复和存储异常，不只验证组件能够渲染。
- 在浏览器检查键盘操作、375px 手机布局和 1440px 桌面布局。
- 按 Spec 的验收编号记录实际结果；无法执行的检查注明原因，不声称通过。
- 当前文档示例尚无应用代码，不能把上述命令描述成已经执行成功。

## 交付要求

- 提供代码变更、必要测试、锁文件和更新后的文档。
- 说明完成内容、验证结果、已知限制与未完成项。
- 更新 Plan 的完成状态，使用其中的执行结果模板汇报。
- 按任务授权提交、推送工作分支并提供审查入口，由负责人完成验收与合并。
